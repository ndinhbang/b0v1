```bash
# Display PHP configuration information
php -i
# List all loaded PHP modules
php -m
# Show PHP version information
php -v
# Show the path to the loaded php.ini file
php --ini
# Test PHP-FPM configuration and show resolved values
php-fpm -tt -y /etc/php/php-fpm.conf
```
